#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    printf("Enter a no. : ");
    scanf("%d",&n);
    if(n%2==0){
        printf("No. is Even");
        }else{
            printf("No. is odd");
            }

    return 0;
}
