#include <stdio.h>
#include <stdlib.h>

int main()
{
    double l,b,a;
printf("Enter the length and breadth of rectangle: ");
scanf("%lf %lf",&l,&b);
a=l*b;
printf("%lf is the required area.",a);
    return 0;
}
